/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'a01a34bca6f20e122e10f1d5e8fd009c.ipfscdn.io',
      // you can add more domains here if needed
    ],
  },
};

export default nextConfig;
